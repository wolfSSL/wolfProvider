name: Stunnel Tests


on:
  workflow_call:
    inputs:
      wolfssl_refs_json:
        description: "JSON array of wolfssl refs to test; empty = use discover_versions output"
        required: false
        type: string
        default: ""
  workflow_dispatch: {}

jobs:
  discover_versions:
    uses: ./.github/workflows/_discover-versions.yml

  build_wolfprovider:
    needs: discover_versions
    uses: ./.github/workflows/build-wolfprovider.yml
    with:
      wolfssl_ref: ${{ matrix.wolfssl_ref }}
      openssl_ref: ${{ matrix.openssl_ref }}
      fips_ref: ${{ matrix.fips_ref }}
      replace_default: ${{ matrix.replace_default }}
    strategy:
      fail-fast: false
      matrix:
        wolfssl_ref: ${{ inputs.wolfssl_refs_json != '' && fromJson(inputs.wolfssl_refs_json) || fromJson(needs.discover_versions.outputs.wolfssl_ref_array) }}
        openssl_ref: ${{ fromJson(needs.discover_versions.outputs.openssl_ref_array) }}
        fips_ref: ["FIPS", "non-FIPS"]
        replace_default: [true]

  test_stunnel:
    runs-on: ubuntu-22.04
    needs: [build_wolfprovider, discover_versions]
    container:
      image: ghcr.io/wolfssl/wolfprovider-test-deps:bookworm
      env:
        DEBIAN_FRONTEND: noninteractive
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        stunnel_ref: ["stunnel-5.67"]
        wolfssl_ref: ${{ inputs.wolfssl_refs_json != '' && fromJson(inputs.wolfssl_refs_json) || fromJson(needs.discover_versions.outputs.wolfssl_ref_array) }}
        openssl_ref: ${{ fromJson(needs.discover_versions.outputs.openssl_ref_array) }}
        fips_ref: ["FIPS", "non-FIPS"]
        force_fail: ['WOLFPROV_FORCE_FAIL=1', '']
        replace_default: [true]
    env:
      WOLFSSL_PACKAGES_PATH: /tmp/wolfssl-packages
      OPENSSL_PACKAGES_PATH: /tmp/openssl-packages
      WOLFPROV_PACKAGES_PATH: /tmp/wolfprov-packages
    steps:
      - name: Checkout wolfProvider
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Download packages from build job
        uses: actions/download-artifact@v4
        with:
          name: debian-packages-${{ matrix.fips_ref }}${{ matrix.replace_default && '-replace-default' || '' }}-${{ matrix.wolfssl_ref }}-${{ matrix.openssl_ref }}
          path: /tmp

      - name: Install wolfSSL/OpenSSL/wolfprov packages
        run: |
          apt install --reinstall -y --allow-downgrades --allow-change-held-packages \
            ${{ env.WOLFSSL_PACKAGES_PATH }}/libwolfssl_*.deb

          apt install --reinstall -y --allow-downgrades --allow-change-held-packages \
            ${{ env.OPENSSL_PACKAGES_PATH }}/openssl_*.deb \
            ${{ env.OPENSSL_PACKAGES_PATH }}/libssl3_*.deb \
            ${{ env.OPENSSL_PACKAGES_PATH }}/libssl-dev_*.deb

          apt install --reinstall -y --allow-downgrades --allow-change-held-packages \
            ${{ env.WOLFPROV_PACKAGES_PATH }}/libwolfprov_*.deb

          # Prevent later 'apt-get install' of test dependencies from
          # replacing the wolfprov-patched libssl3, which breaks 
          # replace-default mode.
          apt-mark hold libssl3 libssl-dev openssl libwolfssl libwolfprov

      - name: Verify wolfProvider is properly installed
        run: |
          $GITHUB_WORKSPACE/scripts/verify-install.sh \
            ${{ matrix.replace_default && '--replace-default' || '' }} \
            ${{ matrix.fips_ref == 'FIPS' && '--fips' || '' }}

      - name: Check Python version
        run: python3 --version

      - name: Checkout Stunnel
        uses: actions/checkout@v4
        with:
          repository: mtrojnar/stunnel
          ref: ${{ matrix.stunnel_ref }}
          path: stunnel
          fetch-depth: 1

      - name: Checkout OSP
        uses: actions/checkout@v4
        with:
          repository: wolfssl/osp
          path: osp
          fetch-depth: 1

      - name: Apply OSP patch to Stunnel
        if: ${{ matrix.stunnel_ref == 'stunnel-5.67' }}
        working-directory: ./stunnel
        run: |
          PATCH=$($GITHUB_WORKSPACE/scripts/resolve-osp-patch.sh $GITHUB_WORKSPACE/osp stunnel WPFF-5.67 ${{ matrix.wolfssl_ref }} ${{ matrix.fips_ref == 'FIPS' && '--fips' || '' }})
          patch -p1 < "$PATCH"

      - name: Build Stunnel
        working-directory: ./stunnel
        run: |
          autoreconf -ivf
          ./configure
          make -j

      - name: Update python cryptography module
        working-directory: ./stunnel
        shell: bash
        run: |
          python3 -m venv myenv
          source myenv/bin/activate

      - name: Verify stunnel with wolfProvider
        working-directory: ./stunnel
        shell: bash
        run: |
          export CRYPTOGRAPHY_OPENSSL_NO_LEGACY=1
          export ${{ matrix.force_fail }}
          source myenv/bin/activate
          ./src/stunnel -version

          # Inline: force-fail crashes before result log exists.
          set +e
          if [ "${{ matrix.force_fail }}" = "WOLFPROV_FORCE_FAIL=1" ]; then
            timeout 30 make check
            TEST_RC=$?
            if [ "$TEST_RC" -eq 0 ]; then
              echo "FAIL: stunnel unexpectedly passed under WOLFPROV_FORCE_FAIL=1"
              exit 1
            fi
            echo "PASS: stunnel failed/timed out as expected (rc=$TEST_RC)"
          else
            timeout 600 make check
            TEST_RC=$?
            if [ "$TEST_RC" -ne 0 ]; then
              echo "FAIL: stunnel tests did not pass in normal mode (rc=$TEST_RC)"
              test -f tests/logs/results.log && tail -50 tests/logs/results.log
              exit 1
            fi
            echo "PASS: stunnel tests passed in normal mode"
          fi
